services:
  calibre-web:
    image: lscr.io/linuxserver/calibre-web:latest
    container_name: calibre-web
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Asia/Kolkata
      - DOCKER_MODS=linuxserver/mods:universal-calibre
    volumes:
      - ~/Documents/calibre-web/config:/config
      - ~/Documents/calibre-web/books:/books
    ports:
      - 8083:8083
    restart: unless-stopped
